package safearray;

public class SafeArray {

	private int a[];
	public int l;
	
	//持失切 五社球
	public SafeArray(int size) {
		a = new int[size];
		l=size;
	}

	public int get(int index) {
		if (index >=0 && index <l) {
			return a[index];
		}
	}
	return -1;
}
